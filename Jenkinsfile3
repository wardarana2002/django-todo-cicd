pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
         sh 'docker build -t todo .'
         sh 'docker run -d -p 8001:8000 todo'
      }
    }
    stage('Test') {
      steps {
       sh 'python3 buildtest.py'
      }
  }
}
}
